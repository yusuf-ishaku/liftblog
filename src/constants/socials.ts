import { linkOptions } from "@tanstack/react-router";

export const socials = linkOptions([
  {
    label: "LinkedIn",
    to: "#" as never,
  },
  {
    label: "Twitter",
    to: "#" as never,
  },
]);
